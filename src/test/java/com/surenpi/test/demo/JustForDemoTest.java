package com.surenpi.test.demo;

import org.junit.Test;

import static org.junit.Assert.assertEquals;
import static org.junit.Assert.assertTrue;

public class JustForDemoTest {
    @Test
    public void one() {
        assertTrue(true);
    }

    @Test
    public void two() {
        assertEquals("", "");
    }
}
